plugins {
    id "com.moowork.node" version "1.1.1"
}

repositories {
    mavenLocal()
    jcenter()
}

task reactbuild( type: NodeTask, dependsOn: 'npmInstall' ) {
    script = file( 'node_modules/react-scripts/bin/react-scripts' )
    args = ['build']
}

task build(dependsOn: 'reactbuild')